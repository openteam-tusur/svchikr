<% if current_page.first? %>
  <%= content_tag :span, raw(t 'views.pagination.previous'), :class => 'prev' %>
<% else %>
  <%= link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => remote, :class => 'prev' %>
<% end %>
